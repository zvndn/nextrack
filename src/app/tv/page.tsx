import { CollectionPage } from "@/components/pages/collection-page";

export default function TvPage() {
  return (
    <CollectionPage
      title="TV Series"
      description="Follow seasons, episodes, paused shows, dropped shows, and what to watch next."
      filter="TV Series"
    />
  );
}
